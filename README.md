# mqttui
CLI TUI for monitoring MQTT messages written in rust using ratatui 

I needed a CLI tool and didn't find one I liked, so here's the one I came up with instead.  It's pretty basic, but does support topic exploration and message filtering.
